import { Router } from "express";
import checkPermission from "../Middleware/auth.middleware";

const router = Router()

// router.get("/fard/:id", checkPermission["ADMIN"], )


export default router;